# dotnet webサーバー

dotnet をつかって、webサーバー,web api サーバー

# 開始

プロジェクトの作成は

```
dotnet new webapi -n dotnet02
```
最後の dotnet02 はプロジェクト名。
その名前のディレクトリが作成される

# 実行確認

http://localhost:表示されたポート/weatherforecast

# staticファイル

../static のファイルを配信するように設定している。